import { Request, Response } from 'express';

const getAllCars = () => {};

const getACar = () => {};

const createACar = () => {};

const updateACar = () => {};

const deleteACar = () => {};

export const carControllers = {
  getAllCars,
  getACar,
  updateACar,
  deleteACar,
  createACar,
};
